const questions = [
  {
    id: 1,
    category: `アルゴリズム`,
    text: `次のプログラム中の [　　] に入れる正しい答えを，解答群の中から選べ。
ある施設の入場料は，0歳から3歳までは100円，4歳から9歳までは300円，10歳以上は500円である。関数feeは，年齢を表す0以上の整数を引数として受け取り，入場料を返す。`,
    code: `〔プログラム〕
○整数型: fee(整数型: age)
  整数型: ret
  if (age が 3 以下)
    ret ← 100
  elseif ( [      ] )
    ret ← 300
  else
    ret ← 500
  endif
  return ret`,
    choices: `ア (age が 4 以上) and (age が 9 より小さい)
イ (age が 4 と等しい) or (age が 9 と等しい)
ウ (age が 4 より大きい) and (age が 9 以下)
エ age が 4 以上
オ age が 4 より大きい
カ age が 9 以下
キ age が 9 より小さい`,
    correct_answer: `カ`,
    explanation: `この問題のポイントは、if 文が上から順番に評価されるという点です。
最初の if (age が 3 以下) がチェックされます。
もし age が 0, 1, 2, 3 であれば、ここで処理が確定し、プログラムは終了します。
elseif の行に来たということは、「age は 3 以下ではない（つまり age は 4 以上である）」ということが既に確定しています。
この時点で age は 4 以上であることが保証されているため、300円の条件「4歳から9歳まで」を満たすために必要な残りの条件は、「9歳以下であること」だけになります。
したがって、[ ] に入る条件は age が 9 以下 となります。`,
    knowledge: `この問題では、以下の知識や論理的思考力が問われています。
条件分岐の順次実行性の理解（排他制御）
if ～ elseif ～ else 構造において、前の条件が false だった場合のみ次の条件が評価されるという仕組みを理解しているか。
「elseif に到達した時点で、前の if の条件（3以下）は除外されている（＝4以上である）」という隠れた前提条件に気づけるかどうかが最大の鍵です。
境界値と不等号の正しい理解
「以下 ($\le$)」「以上 ($\ge$)」「より小さい/未満 ($<$)」「より大きい ($>$)」の違いを正確に区別できるか。
特に、問題文の「9歳まで」を「9 以下」と正しく変換できるかが問われます（「より小さい」を選ぶと9歳が含まれなくなってしまいます）。
論理演算の冗長性の排除
論理的には (age が 4 以上) and (age が 9 以下) と書いても正解ですが、プログラミングにおいては文脈（前の if 文の結果）を利用して、条件式を最もシンプルに記述する能力（冗長な age が 4 以上 を書かない判断）が求められます。
この問題の意図を汲み取ると、「前の条件で弾かれた数値を考慮して、次の条件を最短で書けるか」というアルゴリズムの基礎体力を測っていると言えます。`
  },
  {
    id: 2,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組み合わせを、解答群の中から選べ。ここで、配列の要素番号は1から始まる。
次のプログラムは、整数型の配列arrayの要素の並びを逆順にする。`,
    code: `〔プログラム〕
整数型の配列: array ← {1, 2, 3, 4, 5}
整数型: right, left
整数型: tmp

for (left を 1 から (arrayの要素数 ÷ 2 の商) まで 1 ずつ増やす)
  right ← [  a  ]
  tmp ← array[right]
  array[right] ← array[left]
  [  b  ] ← tmp
endfor`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | arrayの要素数 - left | array[left] |
| イ | arrayの要素数 - left | array[right] |
| ウ | arrayの要素数 - left + 1 | array[left] |
| エ | arrayの要素数 - left + 1 | array[right] |`,
    correct_answer: `ウ`,
    explanation: `この問題は「値の交換（スワップ）」と「配列の添字（インデックス）計算」の2つがポイントです。
① [ b ] の特定（値の交換ロジック）
プログラム後半 of 3行は、array[right] と array[left] の値を入れ替える、典型的な「スワップ処理」です。
tmp ← array[right] （右側の値を退避）
array[right] ← array[left] （右側の場所に、左側の値を入れる）
[ b ] ← tmp （？？？に、退避しておいた右側の値を入れる）
入れ替えを成立させるには、最後に「左側の場所」に退避した値を入れる必要があります。したがって、[ b ] は array[left] となります。この時点で選択肢は ア か ウ に絞られます。
② [ a ] の特定（添字の計算）
問題文に「配列の要素番号は1から始まる」とあります。要素数が 5 の場合を考えてみましょう。
left が 1 のとき、交換相手の right は 5 (末尾) になる必要があります。
選択肢の式に当てはめて検証します（要素数 = 5, left = 1）。
ア： 5 - 1 = 4 （× 不正解。これだと1つズレます）
ウ： 5 - 1 + 1 = 5 （○ 正解）
「要素数 - left」だと、0から始まるインデックスの計算になってしまいます。1から始まる場合は +1 が必要です。`,
    knowledge: `この問題では、以下の知識や論理的思考力が問われています。
スワップアルゴリズムの定石
変数 tmp を介して2つの変数の値を入れ替える「三角交換」の手順（退避→上書き→復帰）を暗記、あるいはトレースして理解できているか。
配列のインデックス（添字）の仕様理解
「0始まり」なのか「1始まり」なのかという要件を読み落とさず、それに応じた数式（境界値）を組み立てられるか。
特に N - i なのか N - i + 1 なのかという「プラス1の有無」は、プログラミング試験で頻出のひっかけポイントです。
ループ範囲の理解
配列を逆順にする際、ループ回数が「要素数 ÷ 2」回（半分まで）で良い理由を理解しているか（全部回すと、一度入れ替わったものが元に戻ってしまいます）。`
  },
  {
    id: 3,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
関数cardinalは，整数型の配列tと桁数m，及び基数nを引数として受け取り，配列tに格納されたm桁のn進数を10進数に変換して返す。
配列tの要素t[1]～t[m]には，m桁のn進数の各桁の数値が，上位桁から順に格納されている。なお，配列tの要素に基数n以上の数値が格納されている場合には，エラーとして (-1) を返す。
関数cardinalに4桁の6進数 "4025" を渡したときの実行例を，図1に示す。
[図1: 省略（t=[4,0,2,5], m=4, n=6 -> 戻り値: 881 (4x6³+0x6²+2x6¹+5x6⁰)）]`,
    code: `〔プログラム〕
○整数型: cardinal(整数型の配列: t, 整数型: m, 整数型: n)
  整数型: i, ret
  i ← 1
  ret ← 0
  while ((ret が (-1) と等しくない) and (i が m 以下))
    if (t[i] が n [  a  ] )
      ret ← [      b      ]
    else
      ret ← (-1)
    endif
    i ← i + 1
  endwhile
  return ret`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | 以下 | (ret + t[i]) × n |
| イ | 以下 | ret × n + t[i] |
| ウ | 以下 | ret × t[i] + n |
| エ | 以下 | ret + t[i] × n |
| オ | より小さい | (ret + t[i]) × n |
| カ | より小さい | ret × n + t[i] |
| キ | より小さい | ret × t[i] + n |
| ク | より小さい | ret + t[i] × n |`,
    correct_answer: `カ`,
    explanation: `この問題は「N進数を10進数に変換するアルゴリズム」と「入力値の妥当性チェック」がテーマです。
① [ a ] の特定（妥当性チェック）
n進数の各桁の数字は、必ず 0 から n-1 の範囲でなければなりません（例：10進数の各桁は0～9。10が入ることはない）。
問題文にも「配列tの要素に基数n以上の数値が格納されている場合にはエラー」とあります。
逆に言えば、正しい計算を行う条件（if文の中身）は、「数値が n より小さい（未満）」場合です。
したがって、t[i] が n より小さい が正解です。これで選択肢は オ～ク に絞られます。
② [ b ] の特定（変換アルゴリズム）
ループを使って上位桁から順番に計算していく手法（ホーナー法）を使っています。
例として、6進数の「4025」を考えます。
1週目（"4"）： 0 * 6 + 4 = 4
2週目（"0"）： 4 * 6 + 0 = 24
3週目（"2"）： 24 * 6 + 2 = 146
4週目（"5"）： 146 * 6 + 5 = 881
このように、「ここまでの合計値 (ret) を n倍して、新しい下の桁 (t[i]) を足す」という処理を繰り返します。
式にすると ret ← ret × n + t[i] となります。`,
    knowledge: `この問題では、以下の知識が問われています。
N進数の仕組みと基数の定義
「N進数の1桁には、Nが含まれない（必ずNより小さい）」という基本ルールの理解。
ホーナー法（Horner's Method）の理解
多項式の計算 $ax^3 + bx^2 + cx + d$ を、 $((a)x + b)x + c)x + d$ という形に変形して、ループ処理で効率よく計算するアルゴリズムの定石を知っているか、あるいはトレースして導き出せるか。
変数の役割の把握（アキュムレータ）
変数 ret が、ループごとに桁上がり（×n）しながら値を累積していく役割を持っていることを見抜く力。`
  },
  {
    id: 4,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを、解答群の中から選べ。
非負の整数nの階乗 (n!) を、図1のように定義する。
[図1: 省略（0!=1, 1!=1, n!=1×2×…×(n-1)×n）]
関数factは、引数で与えられた非負の整数mの階乗を、再帰的に計算して返す。なお、引数mの値が負の場合は、エラーとして (-1) を返す。`,
    code: `〔プログラム〕
○整数型: fact(整数型: m)
  if (m が 0 より大きい)
    return ( [      a      ] )
  elseif ( [      b      ] )
    return 1
  else
    return (-1)
  endif`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | (m - 1) × fact(m) | m が 0 と等しい |
| イ | (m - 1) × fact(m) | m が 1 以下 |
| ウ | (m - 1) + fact(m) | m が 0 と等しい |
| エ | (m - 1) + fact(m) | m が 1 以下 |
| オ | m × fact(m - 1) | m が 0 と等しい |
| カ | m × fact(m - 1) | m が 1 以下 |
| キ | m + fact(m - 1) | m が 0 と等しい |
| ク | m + fact(m - 1) | m が 1 以下 |`,
    correct_answer: `オ`,
    explanation: `この問題は「再帰関数（リカーシブコール）」を用いた階乗計算がテーマです。
① [ a ] の特定（再帰の定義）
階乗の定義 $n! = n \\times (n-1)!$ をプログラムで表現します。
現在の引数 m に対して、m と fact(m-1)（一つ小さい数の階乗）を掛け合わせるのが正解です。
したがって、m × fact(m - 1) となります。
この時点で選択肢は オ・カ・キ・ク に絞られます（足し算のキ・クは除外）。
② [ b ] の特定（停止条件とエラー処理の境界）
プログラムの構造を見てみましょう。
if (m > 0)：正の整数の場合、再帰計算をして潜っていきます。
elseif ( [ b ] )：ここで 1 を返しています。これは $0! = 1$ という定義に対応する「停止条件（ベースケース）」です。
else：ここで -1 を返しています。問題文に「負の場合はエラーとして (-1)」とあるので、ここは負の値が来た時の処理です。
もし [ b ] を「m が 1 以下（カ）」にしてしまうとどうなるでしょうか？
m が -5 の場合、最初の if は偽になり、次の elseif (-5 <= 1) が真になってしまい、エラーであるはずなのに 1 を返してしまいます。
負の数を正しく else に流すためには、[ b ] は負の数を含んではいけません。
したがって、0ピンポイントである m が 0 と等しい が正解となります。`,
    knowledge: `この問題では、以下の知識が問われています。
再帰関数の構造（Recursion）
「自分自身を呼び出す部分（再帰呼び出し）」と「再帰を止める部分（ベースケース）」の2つが必ず必要であるという基本構造の理解。
階乗の数学的定義の実装
漸化式 $f(n) = n \\times f(n-1)$ をコードに落とし込む能力。
境界値分析（バウンダリ値テスト）
条件分岐において、0, 1, -1 などの境界値を入れた時に、仕様（負の数はエラー）通りに動くかを検証する力。「以下」と「等しい」の違いがバグを生む（負の数を巻き込んでしまう）ことに気づけるかが鍵です。`
  },
  {
    id: 5,
    category: `アルゴリズム`,
    text: `次のプログラム中の [　　] に入れる正しい答えを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
関数xorは，整数型の配列bin1，bin2を引数として受け取り，それぞれに格納された8ビットの2進数の排他的論理和（XOR）の結果を返す。
関数xorの実行例を，図1に示す。なお，配列bin1，bin2に0又は1以外の数値が格納されていた場合の結果は保証しなくてよい。
[図1: 省略（bin1=[0,0,1,1,1,0,1,0], bin2=[0,1,0,1,1,1,1,0] -> 結果=[0,1,1,0,0,1,0,0]）]`,
    code: `〔プログラム〕
○整数型の配列: xor(整数型の配列: bin1, 整数型の配列: bin2)
  整数型の配列: retBin
  整数型: i
  for (i を 1 から 8 まで 1 ずつ増やす)
    if ( [      ] )
      retBin[i] ← 1
    else
      retBin[i] ← 0
    endif
  endfor
  return retBin`,
    choices: `ア (bin1[i] × bin2[i]) が 1 と等しい
イ (bin1[i] + bin2[i]) が 1 と等しい
ウ (bin1[i] が 1 と等しい) and (bin2[i] が 1 と等しい)
エ (bin1[i] が 1 と等しい) or (bin2[i] が 1 と等しい)
オ bin1[i] が bin2[i] と等しい`,
    correct_answer: `イ`,
    explanation: `XOR（排他的論理和）の真理値表における出力が「1」になる条件は、入力が「不一致（0と1、または1と0）」の場合です。
ア：論理積（AND）。両方1のときのみ1（積が1）。
イ：入力が0と1の場合、和は1となり条件を満たします。両方0なら和は0、両方1なら和は2となるため、XORの「不一致のときのみ真」という動作と一致します。
ウ：論理積（AND）。アと同じ。
エ：論理和（OR）。どちらか片方でも1なら真となるため、(1, 1)の場合も1になってしまいます。
オ：否定排他的論理和（XNOR）。一致している場合（0と0、1と1）に真となります。
したがって、算術演算を用いてXORの挙動を表現している イ が正解です。`,
    knowledge: `論理演算の真理値表（Truth Table）
XOR（排他的論理和）が「入力が異なるときに真（1）を返す」演算であることを理解しているか。
算術演算による論理演算の代用
0と1のビット演算において、「和が1である」という算術的な条件が、論理的な「XOR」と等価であることに気づける応用力。`
  },
  {
    id: 6,
    category: `アルゴリズム`,
    text: `次のプログラム中の [　　] に入れる正しい答えを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
手続shiftLeftは，整数型の配列binに格納されたビット列と正の整数nを引数として受け取り，ビット列を左にnビット論理シフトする手続である。
ここで，ビット列は下位の桁から順に，bin[1]～bin[binの要素数]に格納されているものとする。また，論理シフトした結果，はみ出したビットは切り捨て，空いたビット位置には0を挿入する。
手続shiftLeftの実行例を，図1に示す。なお，配列binに0又は1以外の数値は格納されていないものとする。
[図1: 省略（bin=[1,0,1,0,0,0,1,1], n=2 -> 左シフト -> bin=[1,0,0,0,1,1,0,0]）]`,
    code: `〔プログラム〕
○shiftLeft(整数型の配列: bin, 整数型: n)
  整数型: i
  for (i を [                  ] )
    bin[i + n] ← bin[i]
  endfor
  for (i を 1 から n まで 1 ずつ増やす)
    bin[i] ← 0
  endfor`,
    choices: `ア (binの要素数 - n) から 1 まで 1 ずつ減らす
イ (binの要素数 - n) から n まで 1 ずつ減らす
ウ 1 から (binの要素数 - n) から 1 ずつ増やす
エ 1 から binの要素数 まで 1 ずつ増やす
オ binの要素数 から 1 まで 1 ずつ減らす
カ binの要素数 から n まで 1 ずつ減らす
キ n から (binの要素数 - n) まで 1 ずつ増やす
ク n から binの要素数 まで 1 ずつ増やす`,
    correct_answer: `ア`,
    explanation: `この問題の核は「インプレース（同一配列内）でのデータ移動における、上書き事故の回避」です。
移動方向の確認
処理は bin[i + n] ← bin[i] です。つまり、インデックスの小さい方（i）から大きい方（i+n）へ値をコピーします。
ループ方向の決定
もしインデックスを「1, 2, 3...」と昇順に処理すると、例えば n=1 の場合、
i=1: bin[2] に bin[1] を書き込む（bin[2]の元の値が消える）
i=2: bin[3] に bin[2] を書き込む（先ほど書き換えた bin[1] の値がコピーされてしまう）
というバグ（データの波及）が発生します。
これを防ぐため、コピー先にまだ参照していないデータがある「後ろ（大きいインデックス）」から順に処理する必要があります。つまり「1ずつ減らす」が必須です。
ループ範囲の決定
開始値（上限）: コピー先の i + n が配列の範囲外（要素数を超える）になってはいけません。
i + n <= 要素数 である必要があるため、i <= 要素数 - n。したがって開始値は binの要素数 - n です。
終了値（下限）: 最も下位のビット bin[1] も移動対象ですので、i は 1 まで回す必要があります。`,
    knowledge: `インプレースアルゴリズムのデータ依存性
コピー元とコピー先が同じ配列領域である場合、ループの方向（昇順か降順か）を誤るとデータを破壊してしまうことへの理解。
「dest > src ならば降順コピー、dest < src ならば昇順コピー」という定石。
配列の添字計算（バウンダリ）
「シフトして溢れる部分は処理しない」という仕様を、ループの初期値 要素数 - n として正しく数式化する能力。`
  },
  {
    id: 7,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。
関数sumOddOrEvenは，正の整数numを引数として受け取り，正の整数numが奇数であれば1～numの奇数を加算した値，偶数であれば2～numの偶数を加算した値を戻り値として返す。
関数sumOddOrEvenの実行例を，図1に示す。なお，引数numが正の整数でない場合は，エラーとして (-1) を返す。
[図1: 省略（num=7 -> 16(1+3+5+7), num=12 -> 42(2+4+...+12)）]`,
    code: `〔プログラム〕
○整数型: sumOddOrEven(整数型: num)
  整数型: sum
  if (num が 0 より大きい)
    sum ← 0
    while ( [      a      ] )
      sum ← sum + num
      num ← [      b      ]
    endwhile
    return sum
  else
    return (-1)
  endif`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | (num mod 2) が 0 と等しい | num - 1 |
| イ | (num mod 2) が 0 と等しい | num - 2 |
| ウ | (num mod 2) が 0 と等しくない | num - 1 |
| エ | (num mod 2) が 0 と等しくない | num - 2 |
| オ | num が 1 以上 | num - 1 |
| カ | num が 1 以上 | num - 2 |
| キ | num が 1 より大きい | num - 1 |
| ク | num が 1 より大きい | num - 2 |`,
    correct_answer: `カ`,
    explanation: `① [ b ] の特定（数列の推移）
奇数の列「7, 5, 3, 1」も、偶数の列「12, 10, 8... 2」も、どちらも共通して 「2ずつ減る」 数列です。
したがって、次項を作る処理は num - 2 となります。
これで選択肢は イ・エ・カ・ク に絞られます。
② [ a ] の特定（ループの継続条件）
ループは「大きい方から足していき、最後に 1（奇数の場合）または 2（偶数の場合）を足して終了」となります。
奇数の場合（最後は num = 1）：
sum に 1 を足すためには、num が 1 の時もループ内に入る必要があります。
num が 1 より大きい（ク）だと、1 > 1 は False なのでループに入らず、最後の 1 が足されません。
num が 1 以上（カ）なら、1 >= 1 は True なのでループに入り、正しく足されます。
偶数の場合（最後は num = 2）：
num が 2 の時にループに入り、sum に足した後、num は 0 になります。次の判定 0 >= 1 で False となり終了します。これはどちらの条件でも正常に動きます。
両方のケースを満たすためには、num が 1 以上 である必要があります。`,
    knowledge: `等差数列のアルゴリズム
奇数列も偶数列も「公差が2である」という共通点に気づき、処理を共通化できるか。
ループの境界条件（Off-by-one errorの回避）
「以上 ($\\ge$)」と「より大きい ($>$)」の違いによって、ループの最後の1回が実行されるかどうかが変わることをシミュレーションできるか。特に「奇数の終了条件（1を含む）」が判断の分かれ目になります。`
  },
  {
    id: 8,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。配列の要素番号は1から始まる。
関数sumBinaryは，引数で与えられた二つの符号なし2進数を加算した演算結果を，符号なし2進数として返す。
ここで，二つの符号なし2進数は，整数型の配列bin1とbin2に下位の桁から順に格納されている。また，配列bin1とbin2に格納されている2進数の桁数（要素数）は同じである。
関数sumBinaryの実行例を，図1に示す。なお，配列bin1，bin2に0又は1以外の数値は格納されていないものとする。また，演算結果を求める整数型の配列sumは，処理中に新しい要素を追加できる可変長配列とする。
[図1: 省略（筆算のイメージ。繰り上がりを考慮して計算している）]`,
    code: `〔プログラム〕
○整数型の配列: sumBinary(整数型の配列: bin1, 整数型の配列: bin2)
  整数型の配列: sum ← {}  /* 要素数0の配列として初期化 */
  整数型: c, i
  c ← 0
  for (i を 1 から bin1の要素数 まで 1 ずつ増やす)
    if (bin1[i] が bin2[i] と等しい)
      [      a      ]
      if (bin1[i] が 1 と等しい)
        c ← 1
      else
        c ← 0
      endif
    else
      if ( [      b      ] )
        sumの末尾 に 0 を追加する
      else
        sumの末尾 に 1 を追加する
      endif
    endif
  endfor
  if (c が 1 と等しい)
    sumの末尾 に 1 を追加する
  endif
  return sum`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | sum의末尾 に 0 を追加する | c が 0 と等しい |
| イ | sumの末尾 に 0 を追加する | c が 1 と等しい |
| ウ | sumの末尾 に 1 を追加する | c が 0 と等しい |
| エ | sumの末尾 に 1 を追加する | c が 1 と等しい |
| オ | sumの末尾 に bin1[i]の値を追加する | c が 0 と等しい |
| カ | sumの末尾 に bin1[i]の値を追加する | c が 1 と等しい |
| キ | sumの末尾 に cの値を追加する | c が 0 と等しい |
| ク | sumの末尾 に cの値を追加する | c が 1 と等しい |`,
    correct_answer: `ク`,
    explanation: `この問題は、1ビット同士の足し算における「合計値（Sum）」と「繰り上がり（Carry）」の関係を整理すれば解けます。
変数 c は繰り上がり（Carry In）を表しています。
① [ a ] の特定（入力ビットが同じ場合）
if (bin1[i] == bin2[i]) ということは、入力は (0, 0) または (1, 1) です。
ここに下からの繰り上がり c を足した結果（Sum）を考えます。
Case (0, 0): $0 + 0 + c = c$ です。追加すべき値は c。
Case (1, 1): $1 + 1 + c = 2 + c$ です。
$c=0$ なら $2$ (2進数で 10) $\\rightarrow$ 1の位は 0 (つまり $c$)。
$c=1$ なら $3$ (2進数で 11) $\\rightarrow$ 1の位は 1 (つまり $c$)。
どちらの場合も、この桁の計算結果（1の位）は c と等しくなります。
よって、[ a ] は sumの末尾 に cの値を追加する です。
（これで選択肢は キ・ク に絞られます）
② [ b ] の特定（入力ビットが異なる場合）
else ブロックに来たということは、入力は (0, 1) または (1, 0) です。
計算式は $1 + 0 + c = 1 + c$ となります。
Case c=0: $1 + 0 + 0 = 1$ $\\rightarrow$ sum に 1 を追加。
Case c=1: $1 + 0 + 1 = 2$ (2進数で 10) $\\rightarrow$ sum に 0 を追加（繰り上がり発生）。
コードでは if ([ b ]) sum...0を追加 となっています。「0を追加する」のは c が 1 の時です。
よって、[ b ] は c が 1 と等しい となります。`,
    knowledge: `全加算器（Full Adder）の論理
入力A, B, CarryIn の3つの値から、Sum（和）と CarryOut（次の桁への繰り上がり）を導く論理回路の動作を、コード上でシミュレーションできるか。
条件網羅的な思考
「入力が同じ場合」「入力が違う場合」×「繰り上がりが有る場合」「無い場合」の4パターンすべてにおいて、正しいビットが出力されるかを確認する力。
変数の状態遷移（Carryの保持）
入力が異なる場合（elseブロック）において、なぜ c の更新処理が書かれていないのか？
$(1+0) + 0 = 1$ (繰り上がりなし$\\rightarrow$ $c$は0のまま)
$(1+0) + 1 = 10_2$ (繰り上がりあり$\\rightarrow$ $c$は1のまま)
「入力不一致の場合は、繰り上がり状態 $c$ が変化しない（入力がそのまま出力になる）」という性質を利用してコードが省略されていることに気づけると、より深く理解できます。`
  },
  {
    id: 9,
    category: `アルゴリズム`,
    text: `次の記述中の [　　] に入れる正しい答えを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
関数divisionは，引数x，yとして与えられた整数に対して演算を施した結果を，整数型の配列resultに格納して返す。関数divisionを，division(9, 4)として呼び出したときの戻り値は，{ [　　] } である。`,
    code: `〔プログラム〕
○整数型の配列: division(整数型: x, 整数型: y)
  整数型の配列: result
  result ← {0, 0}
  if ((x が 0 より大きい) and (y が 0 より大きい))
    result[1] ← x
    while (result[1] が y 以上)
      result[1] ← result[1] - y
      result[2] ← result[2] + 1
    endwhile
  endif
  return result`,
    choices: `ア 0, 0
イ 0, 1
ウ 0, 2
エ 1, 0
オ 1, 1
カ 1, 2
キ 2, 0
ク 2, 1`,
    correct_answer: `カ`,
    explanation: `コードをトレース（追跡）します。division(9, 4) なので、x = 9, y = 4 です。
初期化: result は {0, 0}。
前準備: result[1] ← x なので、配列は {9, 0} になります。
ループ処理: 条件は result[1] (現在9) >= 4。
1回目:
result[1] (余り用) から 4 を引く → 9 - 4 = 5
result[2] (商用) に 1 を足す → 0 + 1 = 1
現状: {5, 1}
2回目: 条件 5 >= 4 なので継続。
result[1] から 4 を引く → 5 - 4 = 1
result[2] に 1 を足す → 1 + 1 = 2
現状: {1, 2}
3回目: 条件 1 >= 4 は False なのでループ終了。
結果: {1, 2} を返します。`,
    knowledge: `除算の原理（減算による実装）
「割り算とは、割られる数から割る数を何回引けるか（商）、そして最後に何が残るか（余り）である」という定義をアルゴリズムとして理解しているか。
このコードでは、result[1] が「余り（mod）」、result[2] が「商（div）」の役割を果たしています。
変数の役割分担の把握
配列のインデックス1と2が、それぞれ計算過程でどのように変化し、最終的に何を意味する変数になっているかをコードから読み取る力。
result[1] が減っていき、result[2] が増えていく対比構造が見どころです。`
  },
  {
    id: 10,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
関数medianは，観測データ（正の整数）が昇順に格納された整数型の配列dataを引数として受け取り，中央値（メジアン）を返す。なお，観測データの個数が偶数の場合，中央に位置する二つの観測データの平均を中央値とする。
関数medianの実行例を，図1に示す。配列dataには1個以上の観測データが格納され，最後の観測データの後は未定義の配列要素がある。また，配列dataに観測データが昇順に格納されていなかった場合は，エラーとして (-1) を返す。
[図1: 省略（data=[12, 18, 18, 23, 25, 30] -> 戻り値: 20 ((18+23)/2=20.5の整数部)）]`,
    code: `〔プログラム〕
○整数型: median(整数型の配列: data)
  整数型: med, num
  med ← 0
  num ← 1
  while (data[num + 1] が 未定義でない)
    if ( [      a      ] )
      med ← (-1)
    endif
    num ← num + 1
  endwhile
  if (med が (-1) と等しくない)
    med ← data[(num + 1) ÷ 2]
    if ((num mod 2) が 0 と等しい)
      med ← (med + [      b      ] ) ÷ 2
    endif
  endif
  return med`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | data[num] が data[num + 1] と等しい | data[num ÷ 2] |
| イ | data[num] が data[num + 1] と等しい | data[num ÷ 2 + 1] |
| ウ | data[num] が data[num + 1] と等しい | data[num ÷ 2 - 1] |
| エ | data[num] が data[num + 1] より大きい | data[num ÷ 2] |
| オ | data[num] が data[num + 1] より大きい | data[num ÷ 2 + 1] |
| カ | data[num] が data[num + 1] より大きい | data[num ÷ 2 - 1] |
| キ | data[num] が data[num + 1] より小さい | data[num ÷ 2] |
| ク | data[num] が data[num + 1] より小さい | data[num ÷ 2 + 1] |
| ケ | data[num] が data[num + 1] より小さい | data[num ÷ 2 - 1] |`,
    correct_answer: `オ`,
    explanation: `① [ a ] の特定（昇順チェック）
「昇順」とは、データが小さい順（または等しい値）に並んでいる状態、つまり $data[num] \\le data[num+1]$ です。
プログラムでは、この規則が守られていない場合にエラー（-1）を設定します。
したがって、エラーとなる条件は「前の要素が、後ろの要素より大きい」場合です。
正解は data[num] が data[num + 1] より大きい となります。
（これで選択肢は エ・オ・カ に絞られます）
② [ b ] の特定（偶数個のメジアン計算）
要素数 num が偶数の場合（例：num = 6）、中央値は「中央の2つの値の平均」です。
中央の2つとは、要素番号 3 と 4 です。
まず、直前の行で med ← data[(num + 1) ÷ 2] が実行されています。
num=6 のとき、(6 + 1) ÷ 2 = 3.5 → 整数除算で 3。
つまり、med には既に data[3] が入っています。
次に、もう片方の data[4] を足して2で割る必要があります。
num=6 を使ってインデックス「4」を作る式を探します。
num ÷ 2 + 1 = 6 ÷ 2 + 1 = 3 + 1 = 4。
よって、[ b ] は data[num ÷ 2 + 1] が正解です。`,
    knowledge: `昇順・降順の定義
不等号の向きと、エラー条件（論理否定）の関係を正しく把握できるか。
配列の中央要素のインデックス計算
データの個数 $N$ が奇数の場合と偶数の場合で、中央のインデックスがどうなるか。
$(N+1)/2$ という式が、整数除算においてどのような挙動（切り捨て）をするか理解しているか。
奇数（N=5）：$6/2 = 3$ （ど真ん中）
偶数（N=6）：$7/2 = 3.5 \\rightarrow 3$ （真ん中2つのうちの前の方）
変数のトレース能力
num という変数がループ終了後に「全要素数」を保持していることを読み取った上で、それを使った数式を組み立てる力。`
  },
  {
    id: 11,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
関数roundは，引数として渡された小数を，指定された位で四捨五入して返す。
小数は，整数型の配列decで渡される。dec[1]の前に小数点があると仮定して，dec[1]に小数第1位，dec[2]に小数第2位，dec[3]に小数第3位，…と各位の数値を順番に格納する。
四捨五入する位は，整数型の変数numで渡される。例えば，変数numで2が渡された場合は，小数第2位で四捨五入し，小数第1位までの実数を返す。
関数roundの実行例を，図1に示す。なお，配列decの要素に0～9以外の数値は格納されておらず，変数numで指定された位の数値は必ず存在する。
[図1: 省略（dec=[2, 4, 6, 9, 5], num=2 -> 戻り値: 0.2, num=5 -> 戻り値: 0.2470）]`,
    code: `〔プログラム〕
○実数型: round(整数型の配列: dec, 整数型: num)
  整数型: i, base
  実数型: ret
  base ← 1
  ret ← 0.0
  for (i を 1 から num まで 1 ずつ増やす)
    if (i が num より小さい)
      ret ← ret × 10.0 + dec[i]
      [      a      ]
    elseif ( [      b      ] )
      ret ← ret + 1.0
    endif
  endfor
  return (ret ÷ base)`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | base ← base × 10 | dec[i] が 5 以上 |
| イ | base ← base × 10 | dec[i + 1] が 5 以上 |
| ウ | base ← base × 10 | i が num と等しい |
| エ | base ← base ÷ 10 | dec[i] が 5 以上 |
| オ | base ← base ÷ 10 | dec[i + 1] が 5 以上 |
| カ | base ← base ÷ 10 | i が num と等しい |
| キ | base ← base + 1 | dec[i] が 5 以上 |
| ク | base ← base + 1 | dec[i + 1] が 5 以上 |
| ケ | base ← base + 1 | i が num と等しい |`,
    correct_answer: `ア`,
    explanation: `このプログラムは、「四捨五入する位の一つ上の位までを整数として取り出し (ret)、最後に桁数分だけ割る (base)」というロジックで動いています。
① [ a ] の特定（桁数の管理）
if (i が num より小さい) のブロックは、四捨五入の対象とならず、結果として残る上位の桁を処理しています。
例えば 0.246... で num=2（小数第2位で四捨五入）の場合、i=1 の時に「2」を ret に取り込みます。
このとき、最終的に小数に戻すための割り算の分母 base も、桁が進むごとに10倍していく必要があります（1桁なら10、2桁なら100で割る）。
よって、base ← base × 10 が入ります。
（これで選択肢は ア・イ・ウ に絞られます）
② [ b ] の特定（四捨五入の判定）
elseif ブロックは、i が num に達した時（ループの最後）に実行されます。
ここでの処理は「四捨五入の判定を行い、繰り上げるなら ret に 1 を足す」ことです。
四捨五入のルールは「対象の位の数字が5以上なら切り上げ」です。
現在見ている桁は i（つまり num の位置）であり、その値は dec[i] です。
よって、判定条件は dec[i] が 5 以上 となります。
検証（トレース）：
num=2、dec=[2, 4, ...] の場合（正解：0.2）
i=1: if内。ret=2, base=10。
i=2: elseif内。dec[2]は4。4 >= 5 はFalse。retは2のまま。
return 2 / 10 = 0.2。正解。
num=5、dec=[..., 9, 5] の場合（正解：0.2470）
i=1～4: retは順に 2→24→246→2469 と成長。baseは 10000 になる。
i=5: elseif内。dec[5]は5。5 >= 5 はTrue。
ret に 1 を足して 2470 になる。
return 2470 / 10000 = 0.2470。正解。`,
    knowledge: `小数の内部表現と生成ロジック
小数を「整数部分（分子）」と「桁数情報（分母）」に分けて計算し、最後に割り算で復元する手法。浮動小数点計算誤差を避けるためにもよく使われる考え方です。
四捨五入のアルゴリズム
「N位で四捨五入」＝「N-1位までの数値に対し、N位の値を見て+1するかどうか決める」という手続きへの理解。
ループ制御と条件分岐
ループ変数 i が num に到達したときだけ別の処理（判定）を行うというフロー制御の読み取り。`
  },
  {
    id: 12,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
関数countDaysは，引数fromとtoに受け取った日付を同じ年の日付とし，二つの日付間の経過日数を返す。経過日数は二つの日付が同じ場合を0日とする。例えば，二つの日付が1月2日と1月5日なら，経過日数は3日になる。
日付は，クラスDateを用いて表現する。クラスDateの説明を図1に示す。Date型の変数はクラスDateのインスタンスの参照を格納するものとする。
[図1: 省略（Dateクラス: month, dayを持つ）]
関数countDaysでは，各月の日数を格納した整数型の配列mDayを使用する。なお，経過日数の計算では，うるう年を考慮しなくてよいものとする。また，引数fromに受け取った日付は，引数toに受け取った日付よりも前の日付とする。`,
    code: `〔プログラム〕
○整数型: countDays(Date: from, Date: to)
  整数型の配列: mDay ← {31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31}
  整数型: i, days
  if (from.month が to.month と等しい)
    days ← to.day - from.day
  else
    days ← [      a      ]
    i ← from.month + 1
    while (i が to.month より小さい)
      days ← days + mDay[i]
      i ← i + 1
    endwhile
    days ← [      b      ]
  endif
  return days`,
    choices: `ア 0 ...
（中略）
キ mDay[from.month] - from.day, days + mDay[i] - to.day
ク mDay[from.month] - from.day, days + to.day
ケ mDay[from.month] - from.day, days - to.day`,
    correct_answer: `ク`,
    explanation: `月がまたがる場合の日数計算は、「(A)開始月の残り日数」＋「(B)間の月の日数」＋「(C)終了月の日数」の合計で求められます。
① [ a ] の特定（開始月の残り日数）
else ブロックに入った時点で、月が異なることが確定しています。
まず、開始日（from）からその月の末日までの日数を求めます。
その月の日数総数 - その月の現在日 で求められるので、
mDay[from.month] - from.day となります。
（例：1月30日開始で1月が31日までなら、31 - 30 = 1日分が1月の残り）
② [ b ] の特定（終了月の日数加算）
while ループで間の月（例えば3月～5月なら、4月分）を足し終わった後、最後に終了月（to）の日付分を足します。
例えば「5月10日」までなら、5月に入ってからの経過日数はそのまま 10（to.day）です。
したがって、累積変数 days に足し込む式は days + to.day となります。`,
    knowledge: `カレンダー計算（日付差分）のアルゴリズム
「開始月の残り ＋ 中間の月全部 ＋ 終了月の経過」という3段階分割のロジック構築力。
配列とオブジェクト（構造体）へのアクセス
mDay 配列から正しい月の日数を引くためのインデックス指定と、Dateオブジェクトのメンバ変数（.day, .month）の使い分け。`
  },
  {
    id: 13,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
手続stockUpdateは，自動販売機を模したクラスDrinkMachineにおいて，客が商品を購入した際の，自動販売機内の貨幣の枚数を更新する手続である。
クラスDrinkMachineの説明を図1に，配列money，stockの例を図2に，それぞれ示す。手続stockUpdateからは，図1に示すクラスDrinkMachineのメンバ変数に直接アクセスできるものとする。
[図1: money(貨幣の種類), stock(保有枚数)の説明]
[図2: money=[500, 100, 50, 10], stock=[16, 0, 32, 214]の例]
手続stockUpdateは，引数として客が購入した商品の価格priceと，客が投入した各貨幣の枚数が格納された整数型の配列paymentを受け取る。ここで，money[i]の貨幣の投入枚数が，payment[i]に格納されている。
手続stockUpdateは，客が投入した貨幣の合計金額と商品の価格の差額から釣銭金額を求め，返却する各貨幣の枚数ができるだけ少なくなるように釣銭の払い方を決める。そして，配列stockの各要素を，釣銭を支払った後の枚数に更新する。
なお，客が投入した貨幣の合計金額が商品の価格以上であることは，事前に検査済みである。また，釣銭金額を支払う貨幣の組合せは必ず存在するものとする。`,
    code: `〔プログラム〕
○stockUpdate(整数型: price, 整数型の配列: payment)
  整数型: i, pay, change, cnt
  pay ← 0
  for (i を 1 から moneyの要素数 まで 1 ずつ増やす)
    pay ← pay + payment[i] × money[i]
    stock[i] ← stock[i] + payment[i]
  endfor
  change ← pay - price
  for (i を 1 から moneyの要素数 まで 1 ずつ増やす)
    cnt ← change ÷ money[i]
    if (stock[i] が cnt より小さい)
      [      a      ]
    endif
    stock[i] ← stock[i] - cnt
    change ← [      b      ]
  endfor`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | cnt ← cnt - 1 | change - cnt × money[i] |
| イ | cnt ← cnt - 1 | change - stock[i] × money[i] |
| ウ | cnt ← cnt - 1 | change mod money[i] |
| エ | cnt ← cnt - stock[i] | change - cnt × money[i] |
| オ | cnt ← cnt - stock[i] | change - stock[i] × money[i] |
| カ | cnt ← cnt - stock[i] | change mod money[i] |
| キ | cnt ← stock[i] | change - cnt × money[i] |
| ク | cnt ← stock[i] | change - stock[i] × money[i] |
| ケ | cnt ← stock[i] | change mod money[i] |`,
    correct_answer: `キ`,
    explanation: `この問題は、お釣りを「大きい硬貨から順に」「在庫がある限り」返していく処理（グリーディ法）です。
① [ a ] の特定（在庫不足時の処理）
変数 cnt は「本来返したい枚数（理想）」です。例えば500円玉を3枚返したいと計算したが、stock[i]（在庫）には1枚しかない、という状況が if (stock[i] < cnt) です。
この場合、返せるのは在庫にある全数だけです。
したがって、実際に返す枚数である cnt を、在庫数 stock[i] に書き換える必要があります。
式は cnt ← stock[i] です。
（これで選択肢は キ・ク・ケ に絞られます）
② [ b ] の特定（残りのお釣り金額の更新）
お釣りの残額 change を更新します。
今回返却した金額は、「実際に返す枚数 cnt」×「硬貨の額面 money[i]」です。
したがって、change ← change - cnt × money[i] となります。
なぜ他の選択肢ではダメなのか？
「mod（あまり）」を使う（ケ）：
在庫が十分にある場合なら change mod money[i] で計算できますが、在庫不足で中途半端にしか返せなかった場合（例：400円返したいが100円玉が2枚しかない）、残り200円をまだ次（50円玉など）で返す必要があります。mod を使うと、返せなかった分が消えてしまうため不適切です。`,
    knowledge: `貪欲法（Greedy Algorithm）
お釣りの計算のように「大きい単位から最適な行動を確定させていく」アルゴリズムの典型例。
制約条件（在庫）のハンドリング
「理想値（計算上の必要数）」と「実測値（物理的な限界）」を比較し、低い方に合わせる（minを取る）ロジックの実装力。
変数のスコープと意味の追跡
cnt という変数が、最初は「理想の枚数」を表し、if 文通過後は「確定した枚数」という意味に変化していることを読み取る力。`
  },
  {
    id: 14,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。ここで，配列の要素番号は1から始まる。
ある商店では，取り扱っている10種類の商品（A～J）の売上を，1点（優）～5点（劣）の評価点に変換して管理している。
手続placeは，これらの商品に対して評価点の高い順に順位付けを行う。商品の評価点と順位の例を，表1に示す。なお，同一評価点の商品には同一順位を付け，次の評価点の順位はその分を考慮して付けるものとする。
[表1: 商品の評価点と順位の例（A:3点-7位, B:2点-3位... E:1点-1位... H:5点-10位など）]
商品の情報は，クラスGoodsを用いて表現する。クラスGoodsの説明を，図1に示す。手続placeに引数として与えられるGoods型の配列productsの要素には，各商品を表すクラスGoodsのインスタンスの参照が格納されている。
[図1: クラスGoodsの説明（name:商品名, score:評価点(1~5), ranking:順位）]`,
    code: `〔プログラム〕
○place(Goodsの配列: products)
  整数型の配列: freq ← {0, 0, 0, 0, 0}  /* 評価点の出現度数 */
  整数型の配列: rank ← {0, 0, 0, 0, 0}  /* 評価点ごとの順位 */
  整数型: i
  /* 出現度数を求める */
  for (i を 1 から productsの要素数 まで 1 ずつ増やす)
    freq[products[i].score] ← freq[products[i].score] + 1
  endfor
  /* 順位を求める */
  rank[1] ← 1
  for (i を 2 から 5 まで 1 ずつ増やす)
    rank[i] ← [      a      ]
  endfor
  /* 順位を格納する */
  for (i を 1 から products of 要素数 まで 1 ずつ増やす)
    products[i].ranking ← [      b      ]
  endfor`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | rank[i] + 1 | rank[i] |
| イ | rank[i] + 1 | rank[products[i].score] |
| ウ | rank[i] + freq[i] | rank[i] |
| エ | rank[i] + freq[i] | rank[products[i].score] |
| オ | rank[i - 1] + freq[i] | rank[i] |
| カ | rank[i - 1] + freq[i] | rank[products[i].score] |
| キ | rank[i - 1] + freq[i - 1] | rank[i] |
| ク | rank[i - 1] + freq[i - 1] | rank[products[i].score] |`,
    correct_answer: `ク`,
    explanation: `① [ a ] の特定（順位の累積計算）
まず、順位付けのルールを確認しましょう。
評価点1（最高評価）は、必ず「1位」から始まります（rank[1] ← 1）。
評価点2の順位は、評価点1だった商品の**個数（人数）**分だけ後ろにずれます。
例：評価点1が「2個」あった場合、1位、1位、となり、次は「3位」になります。
計算式：rank[2] = rank[1] + freq[1] （1 + 2 = 3位）
同様に、評価点 i の順位 rank[i] は、「一つ前の評価点 i-1 の順位」に「一つ前の評価点 i-1 だった商品の個数」を足したものです。
よって、rank[i] ← rank[i - 1] + freq[i - 1] となります。
（この時点で選択肢は キ・ク に絞られます）
② [ b ] の特定（各商品への順位割り当て）
最後に、計算しておいた rank 配列を使って、各商品に順位を書き込みます。
商品 products[i] の評価点は products[i].score です。
その評価点に対応する順位は rank[products[i].score] に格納されています。
例えば、商品Aが3点なら、rank[3] の値（7位）を引っ張ってくる必要があります。
よって、products[i].ranking ← rank[products[i].score] が正解です。`,
    knowledge: `度数分布（Frequency Distribution）の作成
データの値をインデックスとして、その出現回数を配列に数え上げる手法（ヒストグラム作成やバケットソートの基礎）。
累積和（Cumulative Sum）による順位決定
「自分より上位の人数」を累積していくことで、自分の順位を決定するアルゴリズム。
NextRank = CurrentRank + Count という漸化式の理解。
オブジェクト配列と間接参照
products[i].score で得た値をさらにインデックスとして rank[...] を引く、という「値の連鎖的な参照」をコードで表現できるか。`
  },
  {
    id: 15,
    category: `アルゴリズム`,
    text: `次のプログラム中の [ a ] と [ b ] に入れる正しい答えの組合せを，解答群の中から選べ。
関数checkDigitは，引数で与えられた3桁のコード "nnn" の末尾に，チェックディジットCを付けた4桁のコード "nnnC" を生成して返す。
ここで，3桁のコードは100～999であり，各桁の値について，百の位を $N_3$，十の位を $N_2$，一の位を $N_1$ としたとき，次のようにチェックディジットCを求める。
$C = (N_3 \\times 4 + N_2 \\times 2 + N_1) \\text{ mod } 7$
例えば，3桁のコード "457" の場合，チェックディジットCは5になる。
$C = (4 \\times 4 + 5 \\times 2 + 7) \\text{ mod } 7$
$= (16 + 10 + 7) \\text{ mod } 7$
$= 33 \\text{ mod } 7$
$= 5$   ※ $33 \\div 7 = 4$ 余り 5
したがって，関数checkDigitは，3桁のコード "457" を引数として受け取ると，4桁のコード "4575" を生成して返す。`,
    code: `〔プログラム〕
○整数型: checkDigit(整数型: code)
  整数型: i, cnt, tmp, N, C
  tmp ← code
  cnt ← 1
  C ← 0
  for (i を 1 から 3 まで 1 ずつ増やす)
    N ← tmp mod 10
    C ← C + (N × cnt)
    [      a      ]
    tmp ← tmp ÷ 10
  endfor
  C ← C mod 7
  return [      b      ]`,
    choices: `| | a | b |
| :--- | :--- | :--- |
| ア | cnt ← cnt × 2 | C |
| イ | cnt ← cnt × 2 | code × 10 + C |
| ウ | cnt ← cnt × 2 | code + C |
| エ | cnt ← cnt × i | C |
| オ | cnt ← cnt × i | code × 10 + C |
| カ | cnt ← cnt × i | code + C |
| キ | cnt ← cnt + 1 | C |
| ク | cnt ← cnt + 1 | code × 10 + C |
| ケ | cnt ← cnt + 1 | code + C |`,
    correct_answer: `イ`,
    explanation: `この問題は、桁ごとの重み付け計算（ウェイト計算）と、数値の結合がテーマです。
① [ a ] の特定（重みの更新）
ループは tmp mod 10 を使っているため、下の桁（一の位）から順番に処理されます。
問題文の式 $C = N_3 \\times 4 + N_2 \\times 2 + N_1 \\times 1$ を、処理順（一の位→十の位→百の位）に並べ替えると以下のようになります。
1周目 ($N_1$：一の位)：重みは 1 （初期値 cnt = 1 でOK）
2周目 ($N_2$：十の位)：重みは 2 にしたい
3周目 ($N_3$：百の位)：重みは 4 にしたい
重み cnt の変化は 1 → 2 → 4 となっています。これは「2倍ずつ増える（等比数列）」変化です。
したがって、cnt ← cnt × 2 が正解です。
② [ b ] の特定（4桁コードの生成）
最後に、元の3桁 code（例：457）と、計算した C（例：5）を合体させて、4桁の整数（例：4575）を作ります。
整数の末尾に数字をくっつける方法は「10倍して足す」です。
$457 \\times 10 + 5 = 4570 + 5 = 4575$
よって、code × 10 + C となります。`,
    knowledge: `各桁の抽出ロジック
mod 10 で一の位を取り出し、÷ 10 で桁をずらすという、整数処理の基本テクニック。
数値による文字列操作的処理
文字列連結ではなく、数値演算（×10 + n）を使って桁を増やす手法の理解。
チェックディジットの目的
入力ミスや読み取りミスを検出するために付与される、データの妥当性（インテグリティ）を保証するための冗長ビットの概念。`
  }
];